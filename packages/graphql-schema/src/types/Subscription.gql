enum ExchangeEnum {
  RADAR_RELAY
  ETHER_DELTA
}

type Subscription {
  price(symbol: Symbol!): String!
  aggregatedOrderbook(
    baseTokenAddress: String!
    quoteTokenAddress: String!
    exchanges: [ExchangeEnum]
  ): [Order]
}
